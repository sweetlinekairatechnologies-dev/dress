'use client';

import CatalogPageLayout from '../components/CatalogPageLayout';

export default function OffersPage() {
  return (
    <CatalogPageLayout
      title="Offers"
      subtitle="Grab active deals and discounted picks before stock ends."
      type="offers"
    />
  );
}
