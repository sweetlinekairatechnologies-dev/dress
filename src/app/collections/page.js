'use client';

import CatalogPageLayout from '../components/CatalogPageLayout';

export default function CollectionsPage() {
  return (
    <CatalogPageLayout
      title="Collections"
      subtitle="Seasonal edits and curated collections for every style."
      type="all"
    />
  );
}
