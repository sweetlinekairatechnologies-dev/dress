// Minimal Sidebar stub: rendering null removes the sidebar across the app without
// modifying individual pages. This keeps the app layout intact while hiding
// the left navigation as requested.

'use client';

import React from 'react';

export default function Sidebar() {
  return null;
}
